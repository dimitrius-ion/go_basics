package main


func getUser (id int) (string, error) {
	//get user

	if ok {
		return user, nil
	} else {
		return nil, errors.New("User not found")
	}
}

func init() {
	
	user, err := getUser(123)

}